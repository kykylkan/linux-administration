# Провайдери kubernetes/helm успадковуються від root-модуля (передаються неявно
# через required_providers). Явних provider-блоків тут не оголошуємо, щоб
# уникнути дублювання конфігурації - вони налаштовані в кореневому main.tf.
